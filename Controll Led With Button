int ledPin = 13;
int buttonPin = 2;

void setup() {

  pinMode(13, OUTPUT);
  pinMode(2, INPUT);
}

void loop() {
  
  int buttonState = digitalRead(2);

  
  if (buttonState == HIGH) {
    digitalWrite(13, HIGH);
  } else {
   
    digitalWrite(13, LOW);
  }
}
